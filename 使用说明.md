# fpro Cloud Studio 部署与 SSH 密钥交付——操作说明

> 这份文件只记录操作者需要执行的步骤。尖括号中的内容（例如
> `<FPRO_SERVER_HOST>`）必须替换为你自己的安全配置；不要把真实地址、CA
> 名称、token、密码或私钥回填到本文件，也不要提交到 Git。

## 先看结论

推荐的首次部署链路如下：

```text
Windows 本机启动一次性接收器
        │  临时 fpro TCP 端口
        ▼
Cloud Studio 执行 install.sh
        │  生成 SSH 密钥，只发送加密 .enc 包
        ▼
Windows 本机校验、解密并写入 ~/.ssh
        │
        └── 临时 fpro 客户端和接收器自动退出
```

成功路径不需要手动下载私钥。配置包密码不会通过接收器发送；网络上只传输
加密的 SSH 密钥包。

## 一、需要准备的内容

### Cloud Studio 终端

- 能执行 Bash，并可用 `sudo`/root 权限。
- 能访问 GitHub Raw（仓库为公开仓库时不需要 GitHub 登录）。
- `fpro-deploy.tar.gz.enc` 的解压密码。安装脚本会在终端中隐藏式询问。
- 一个服务端允许使用、当前未占用的临时远端端口，供“密钥接收通道”使用。

### Windows 本机

- Python 3、OpenSSL、`ssh-keygen`。PowerShell 中可先检查：

  ```powershell
  Get-Command python,openssl,ssh-keygen
  python --version
  ```

- 与 Windows 主机架构匹配的明文 `fpro-client` 可执行文件。常见情况是
  `fpro-client_windows_amd64.exe`；如果在 WSL 中运行接收器，则应使用 Linux
  版本。
- fpro mTLS 文件：`client.crt`、`client.key`、`ca.crt`。它们是 fpro 客户端
  连接服务端的材料，不是 SSH 登录私钥。
- 本地安全目录，用于保存一次性 token、密码文件和最终的 SSH 私钥。不要把
  这些文件放进同步盘、共享工作区或 Git 仓库。

### 两种 token/密钥不要混淆

1. 加密载荷中的 fpro 认证材料（安装脚本使用，不能写进文档）。
2. 接收器的一次性传输 token（本机临时生成，仅用于本次 POST）。

`certs/client.key` 是 fpro mTLS 私钥；`~/.ssh/fpro-cloudstudio` 是登录容器
的 SSH 私钥，两者用途完全不同。

## 二、首次部署（没有可用 SSH 私钥时，推荐）

### 1. 在 Windows 本机准备私密文件

以下示例把文件放到当前用户目录下的私密目录。路径可按实际情况修改：

```powershell
$secretDir = Join-Path $HOME '.fpro-secrets'
New-Item -ItemType Directory -Force $secretDir | Out-Null

$tokenFile = Join-Path $secretDir 'receiver-token.txt'
$packagePasswordFile = Join-Path $secretDir 'package-password.txt'

# 生成一次性传输 token；token 不会显示在命令行中
python -c "from pathlib import Path; import secrets; Path(r'$tokenFile').write_text(secrets.token_urlsafe(32) + '\n', encoding='utf-8')"

# 隐藏输入配置包密码，并以 UTF-8 保存（不要把密码写入命令本身）
python -c "from pathlib import Path; from getpass import getpass; Path(r'$packagePasswordFile').write_text(getpass('配置包密码: '), encoding='utf-8')"
```

请确认这两个文件只允许当前用户读取。安装完成后，token 文件可以删除；密码
文件可移入密码管理器或安全删除。文件内容不要带额外空格。

### 2. 准备 mTLS 文件

如果本机仓库目录中有未纳入 Git 的 `payload/certs/`，可以直接指定：

```powershell
$repo = 'C:\path\to\fpro-deploy'
$certDir = Join-Path $repo 'payload\certs'
$clientCrt = Join-Path $certDir 'client.crt'
$clientKey = Join-Path $certDir 'client.key'
$caCrt = Join-Path $certDir 'ca.crt'
```

`payload/` 是明文敏感目录，只能留在本机受保护位置，不能上传。若本机没有
`payload/`，在一个不共享的临时目录中解开配置包，仅取出上述三个文件：

```powershell
$repo = 'C:\path\to\fpro-deploy'
$stage = Join-Path $env:TEMP ('fpro-payload-' + [guid]::NewGuid().ToString('N'))
New-Item -ItemType Directory -Force $stage | Out-Null

openssl enc -d -aes-256-cbc -pbkdf2 `
  -in (Join-Path $repo 'fpro-deploy.tar.gz.enc') `
  -out (Join-Path $stage 'payload.tar.gz') `
  -pass "file:$packagePasswordFile"

tar -xzf (Join-Path $stage 'payload.tar.gz') -C $stage
$payloadRoot = if (Test-Path (Join-Path $stage 'payload')) {
  Join-Path $stage 'payload'
} else { $stage }
$certDir = Join-Path $payloadRoot 'certs'
$clientCrt = Join-Path $certDir 'client.crt'
$clientKey = Join-Path $certDir 'client.key'
$caCrt = Join-Path $certDir 'ca.crt'
```

如果 Windows 没有 `tar`，请在 Git Bash 或 WSL 中完成这一步。不要用文本编辑
器打开或改写 `client.key`。接收器退出后删除 `$stage`；确认路径确实是刚才
创建的临时目录后再清理。

### 3. 在 Windows 本机启动一次性 fpro 接收器

先设定仓库、二进制和证书路径：

```powershell
$repo = 'C:\path\to\fpro-deploy'
$fproBinary = 'D:\path\to\fpro-client_windows_amd64.exe'
$receiverTokenFile = Join-Path $secretDir 'receiver-token.txt'
$packagePasswordFile = Join-Path $secretDir 'package-password.txt'
$sshDir = Join-Path $HOME '.ssh'
```

然后在仓库根目录运行（把占位符替换为服务端的安全配置）：

```powershell
Set-Location $repo
python .\tools\fpro_ssh_receiver.py proxy `
  --fpro-binary "$fproBinary" `
  --server-addr '<FPRO_SERVER_HOST>' `
  --server-port <FPRO_CONTROL_PORT> `
  --remote-port <UNUSED_REMOTE_PORT> `
  --tls-cert "$clientCrt" `
  --tls-key "$clientKey" `
  --tls-ca "$caCrt" `
  --tls-server-name '<TLS_SERVER_NAME>' `
  --token-file "$receiverTokenFile" `
  --password-file "$packagePasswordFile" `
  --ssh-dir "$sshDir" `
  --key-name fpro-cloudstudio `
  --response-grace 3
```

说明：

- `<UNUSED_REMOTE_PORT>` 是临时“密钥接收”端口，必须是服务端允许且未被占用
  的端口；它不一定是 SSH 登录端口。
- `--tls-server-name` 应填写证书校验所需的名称；如果证书使用的就是服务端
  地址，可按实际情况填写相同值。
- 接收器默认只监听本机回环地址，不要添加 `--allow-public`。
- 终端出现“临时 fpro 通道已建立”后保持窗口运行，不要先按 Ctrl+C。程序会
  显示本次的 `FPRO_SSH_RECEIVER_URL` 和 token，但 token 已从文件读取；只把
  URL/token 临时传给 Cloud Studio，不要写入 Git、工单或截图。

如果服务端拒绝临时端口，换一个仍在允许范围内的未占用端口；不要复用正在
承载 SSH 的端口。

### 4. 在 Cloud Studio 终端执行安装

把接收器输出的 URL 临时记下。推荐把 token 写入容器内权限为 `600` 的文件，
避免 token 出现在命令行历史中：

```bash
umask 077
read -r -s FPRO_TRANSFER_TOKEN
printf '%s\n' "$FPRO_TRANSFER_TOKEN" > /tmp/fpro-receiver-token
unset FPRO_TRANSFER_TOKEN
```

然后执行 GitHub Raw 安装脚本。将 `<RECEIVER_URL>` 替换为接收器刚打印的 URL：

```bash
curl -fsSL 'https://raw.githubusercontent.com/bot0577/fpro-cloudstudio-deploy/main/install.sh' | sudo env \
  FPRO_SSH_GENERATE=1 \
  FPRO_SSH_RECEIVER_URL='<RECEIVER_URL>' \
  FPRO_SSH_RECEIVER_TOKEN_FILE=/tmp/fpro-receiver-token \
  FPRO_SSH_RECEIVER_TIMEOUT=90 \
  bash
```

安装脚本会依次完成：

1. 从加密配置包询问并校验解压密码。
2. 读取 `uname -s`、`uname -m`，自动下载当前 Linux 架构的独立加密客户端。
3. 安装 fpro 客户端、证书、配置和看门狗。
4. 配置 `sshd`：root 仅允许公钥登录，关闭密码登录。
5. 生成 Ed25519 SSH 密钥，把公钥加入 `authorized_keys`。
6. 用加密包把私钥发送到一次性接收器，并在 Windows 本机校验、解密、写入
   `~/.ssh/fpro-cloudstudio`。
7. 检查 fpro 进程和实际映射端口。端口能建立 TCP 连接才算映射成功；端口
   响应本身不等于已经拥有 SSH 私钥。

默认情况下，SSH 私钥导出密码就是安装时输入的配置包密码。因此 Windows
接收器的 `--password-file` 应保存同一个密码。如果显式设置了
`FPRO_SSH_EXPORT_PASSWORD`，则 `--password-file` 必须改为该单独的导出密码。

### 5. 判断成功并连接

两边都应看到类似结果：

- Cloud Studio：`隧道端口已响应`、`加密 SSH 私钥包已由本机接收器接收`、
  `部署完成`。
- Windows：`接收并解密完成`、`私钥已写入`、SSH 指纹（`SHA256:...`）。
- 临时 fpro 客户端随后自动退出；接收器也只接受这一次上传。

使用 Cloud Studio 安装输出的实际隧道主机和 SSH 映射端口连接。不要把临时
接收端口当成 SSH 端口：

```powershell
ssh.exe -o IdentitiesOnly=yes `
  -i "$HOME\.ssh\fpro-cloudstudio" `
  -p <SSH_MAPPED_PORT> root@<SSH_TUNNEL_HOST>
```

检查本地指纹（应与安装输出一致）：

```powershell
ssh-keygen -lf "$HOME\.ssh\fpro-cloudstudio.pub"
```

首次连接时核对 SSH 主机指纹；不要因为端口响应就跳过主机身份确认。

### 6. 收尾清理

确认 Windows 已写入私钥、Cloud Studio 安装成功后：

```bash
rm -f /tmp/fpro-receiver-token
```

在 Windows 本机：

- 删除一次性 `receiver-token.txt`。
- 如果从配置包临时解出了 `$stage`，确认接收器已退出后删除该临时目录。
- 不要删除 `~/.ssh/fpro-cloudstudio`，它是之后登录所需的私钥；只备份到受保护
  的密码管理器或离线介质。
- 不要把解密后的私钥、`client.key`、auth token 或 bridge 配置复制回 Git 工作区。

## 三、已有 SSH 私钥时的流程

如果你已经有一对本机 SSH 密钥，优先只发送公钥，不发送私钥：

1. 把 `.pub` 文件安全地放入 Cloud Studio 容器（文件权限设为 `600`/`644`）。
2. 在容器中执行：

   ```bash
   sudo env \
     FPRO_AUTHORIZED_KEYS_FILE=/path/to/id_ed25519.pub \
     bash install.sh
   ```

3. 脚本会安装公钥并启动 sshd；本机保留对应私钥即可登录。

如果私钥已经以受保护方式存在容器内，并且你希望安装时做严格 SSH 自检，才额外
设置下面两个变量；脚本会先比较公钥/私钥指纹，不会把私钥写入 Git：

```bash
sudo env \
  FPRO_AUTHORIZED_KEYS_FILE=/path/to/id_ed25519.pub \
  FPRO_SSH_PRIVATE_KEY=/path/to/id_ed25519 \
  FPRO_TUNNEL_SSH_KEY=/path/to/id_ed25519 \
  bash install.sh
```

## 四、容器重启或重新部署

- **容器文件仍保留、原 SSH 公钥仍在**：重新运行安装脚本即可；保留本机原私钥，
  不要无必要地重新生成新密钥。
- **容器被重建、没有原公钥**：重新启动一次性接收器，再按“首次部署”流程使用
  `FPRO_SSH_GENERATE=1`。每次都生成新的 token，不要复用旧 token。
- **只想重新安装客户端**：不设置 `FPRO_SSH_GENERATE=1`，并提供现有公钥；
  `FPRO_TUNNEL_SSH_KEY` 可用于严格 SSH 自检。
- 脚本会按 `uname -m` 自动选择客户端，无需手工选择所有平台二进制：

  | `uname -m` | 使用的加密文件 |
  | --- | --- |
  | `x86_64` / `amd64` | `fpro-client_linux_amd64.b64.enc` |
  | `aarch64` / `arm64` | `fpro-client_linux_arm64.b64.enc` |
  | `armv7*` | `fpro-client_linux_arm_armv7.b64.enc` |
  | `armv5*` / `armv6*` | `fpro-client_linux_arm_armv5.b64.enc` |
  | `loongarch64` | `fpro-client_linux_loong64.b64.enc` |
  | `riscv64` | `fpro-client_linux_riscv64.b64.enc` |
  | `mips*` | 对应的 mips 加密文件 |

如遇未列出的架构，可显式设置 `FPRO_BINARY_NAME`，但文件必须是仓库中的
`fpro-client_<平台名>...enc`，且仍需使用同一解压密码。

## 五、自动接收失败时的安全回退

如果临时通道不可用，仍可以让容器生成加密包，再手动把 `.enc` 文件取到本机：

```bash
sudo env \
  FPRO_SSH_GENERATE=1 \
  FPRO_SSH_EXPORT_PATH=/workspace/fpro-cloudstudio-key.tar.gz.enc \
  bash install.sh
```

然后在 Windows 本机使用仓库中的接收器解密安装（推荐，不要直接把私钥解压到
共享工作区）：

```powershell
python .\tools\fpro_ssh_receiver.py decrypt `
  --input 'C:\path\to\fpro-cloudstudio-key.tar.gz.enc' `
  --password-file "$packagePasswordFile" `
  --ssh-dir "$HOME\.ssh" `
  --key-name fpro-cloudstudio
```

如果目标文件已存在，工具默认拒绝覆盖；只有确认要替换时才加 `--force`。解密
成功后立即删除 Cloud Studio 工作区里的 `.enc` 文件及本机临时副本。

## 六、常见故障处理

### `隧道端口未响应`

这表示 fpro 客户端或映射尚未就绪，安装不会报告成功。检查：

```bash
pgrep -a fpro-client
tail -n 50 /var/log/fpro-watchdog.log
tail -n 50 /var/log/fpro-client.log
```

确认加密配置中的映射端口、服务端允许的端口和网络策略；不要只看进程是否
存在。

### `临时 fpro 端口未上线`

检查 Windows 接收器的 fpro 日志、服务端控制端口、TLS 三个文件、
`--tls-server-name` 和临时远端端口是否正确。更换一次性 token 后重新启动
接收器再试。

### HTTP 401 或 token 无效

接收器 token 与容器传入的 token 不一致。不要重复猜测；关闭当前接收器，生成
新的 token，重新走一次流程。

### HTTP 422、解密失败或密码错误

确认接收器 `--password-file` 与安装时输入的密码完全一致。若设置了
`FPRO_SSH_EXPORT_PASSWORD`，接收器必须使用导出密码而不是配置包密码。

### `目标密钥已存在`

这是防止误覆盖现有登录密钥的保护。确认目标指纹后，可以改用新的
`--key-name`，或明确添加 `--force`；不要在不核对指纹时覆盖。

### SSH `Permission denied (publickey)`

核对三项：

1. `ssh -i` 指向接收器实际写入的私钥。
2. 私钥 `.pub` 指纹与 Cloud Studio 安装输出一致。
3. 使用的是 SSH 映射端口，而不是临时密钥接收端口。

### Cloud Studio/bridge 返回 403 或工作空间处于停止状态

先在 Cloud Studio 页面恢复/启动工作空间，再重启 fpro 客户端和一次性接收器。
这属于运行环境状态，不需要把真实 bridge 配置写入仓库或本说明。

## 七、安全检查清单

- [ ] 本文件只使用占位符，没有真实 IP、CA 名称、token、密码或私钥。
- [ ] 每个平台二进制单独加密上传，不能把所有平台合并成一个包。
- [ ] `payload/`、`.bridge_env.json`、明文证书/私钥、密码文件均未加入 Git。
- [ ] 临时接收器只绑定 `127.0.0.1`，使用一次性 token，并在成功后退出。
- [ ] SSH 私钥最终只在本机 `~/.ssh` 保存；容器端仅短暂存在于脚本临时目录，
      共享工作区只允许短暂存在加密 `.enc`。
- [ ] 之前在聊天中暴露过的 GitHub PAT 必须立即撤销并轮换，不能再使用。

## 八、维护者更新文件时的原则（非日常操作）

普通部署者不需要手工选择或上传二进制。维护者更新时应遵守：

1. 配置载荷单独生成 `fpro-deploy.tar.gz.enc`。
2. 每个平台的原始客户端/服务端文件分别用 AES-256-CBC + PBKDF2 加密，保留
   平台文件名，仅增加 `.enc` 后缀。
3. Git 只提交脚本、文档和加密文件；明文二进制、私钥、auth token、bridge
   配置不得提交。
4. 更新后在目标容器检查 `uname -m` 自动选择是否正确，并做一次端口存活检查
   及一次 SSH 公钥登录验证。
