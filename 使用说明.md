# FProCloudStudio 使用说明（手动执行 Cloud Studio 命令）

## 日常操作

1. 登录 Cloud Studio，打开目标工作区并进入容器终端。终端只需要保持可用，不需要开启浏览器 CDP 或其他自动化功能。
2. 双击 `FProCloudStudio.exe`。
3. 在程序中输入一次“密钥传输密码”，点击“启动临时通道”。这是本机程序唯一必填项；密码不会写入设置文件。
4. 配置准备完成后，“复制 bash 命令”和“复制一次性 token”按钮会变为可用；命令区域会显示完整命令。通道就绪后命令还会自动复制一次。
5. 等待状态显示通道已建立，再把命令粘贴到 Cloud Studio 容器终端并回车；如剪贴板被其他内容覆盖，点击“复制 bash 命令”即可再次复制。这里必须由操作者手动执行，程序不会操作浏览器或终端。
6. 终端提示 `FPRO package password` 时，输入第 3 步的同一个密码；提示 `FPRO one-time transfer token` 时，点击程序中的“复制一次性 token”，再粘贴并回车。命令提示使用 ASCII，以避免终端编码差异导致乱码。
7. 等待容器脚本完成。程序收到加密 SSH 密钥包后会校验 token、SHA-256、SSH 公钥指纹和私钥/公钥匹配关系，解密并原子写入：

   ```text
   %USERPROFILE%\.ssh\fpro-cloudstudio
   %USERPROFILE%\.ssh\fpro-cloudstudio.pub
   ```

8. 程序显示“完成”后，使用日志给出的主机和映射端口连接。示例：

   ```powershell
   ssh -o IdentitiesOnly=yes -i "$env:USERPROFILE\.ssh\fpro-cloudstudio" -p <映射端口> root@<隧道主机>
   ```

连接使用 Ed25519 私钥，不需要输入 Linux 密码。端口响应只代表映射存活；本地密钥写入成功才代表密钥交付完成。

## 程序与容器各自负责什么

本机 EXE 负责：

- 下载并解密配置，以及按 Windows 架构选择的加密 fpro 客户端；
- 启动本机回环文件接收器和一次性 fpro 临时通道；
- 生成本次接收用的随机 token；
- 接收加密密钥包，校验 SHA-256/指纹，解密并保存 SSH 密钥；
- 接收完成或超时后关闭临时通道并清理临时文件。

Cloud Studio 容器中的 `install.sh` 负责：

- 通过 GitHub Raw HTTPS 外链获取脚本、加密配置和当前 `uname -m` 对应的 Linux fpro 客户端；
- 先停止旧的 fpro 看门狗和客户端，再安装当前实例；
- 生成 Ed25519 root SSH 密钥（`FPRO_SSH_GENERATE=1`），把公钥写入 `/root/.ssh/authorized_keys`，并关闭 SSH 密码登录；
- 启动或重新加载 sshd（root 密钥登录、关闭密码登录）和 fpro 隧道，将容器 22 端口映射到配置中的远端端口；
- 先检查映射端口是否存活，再把加密 SSH 私钥包通过一次性通道 POST 回本机。

脚本的默认端口成功标准是 TCP 端口可连接，不会因为没有预先存在的私钥而误报失败。

## 常见情况

- **日志出现乱码**：确认使用本次生成的 EXE；程序已对接收器输出统一使用 UTF-8，并兼容旧版 Windows GB18030 输出。bash 命令中的交互提示也使用 ASCII。
- **复制按钮仍是灰色**：说明配置还在下载/解密，或准备阶段已经报错；等待日志完成，若出现“失败”请按错误提示处理后重新启动。准备完成后两个按钮会在通道健康检查结束前就启用。
- **命令复制失败**：在命令区域点击“复制 bash 命令”；也可以从命令框选中后复制。token 只能使用一次。
- **目标密钥已存在**：为避免覆盖，打开高级设置换一个 SSH 密钥名后重试。
- **临时 fpro 端口未上线**：检查阿里云服务端是否在线、远端临时端口是否允许使用；换一个未占用端口后重试。
- **端口已响应但 SSH 不能登录**：确认程序日志显示密钥已写入，并使用日志中的私钥路径、主机和映射端口；不要把临时接收端口当作 SSH 端口。
- **安装中断**：临时接收器是一次性的。停止后重新启动 EXE，并在容器重新执行新生成的命令和 token。

## 安全注意

- 密钥传输密码只在本次运行的内存和受限临时文件中使用，不保存到设置文件；安装命令本身不包含密码。
- 一次性 token 只用于当前密钥包 POST，不能重复使用，也不要写入 Git、README 或聊天记录。
- 仓库只允许提交加密载荷和按平台分别加密的二进制；明文客户端私钥、auth token、CA 私钥、bridge 配置和解密后的 SSH 私钥不得提交。
- 收到密钥后不要把未加密私钥复制回共享工作区；如需重新部署，重新生成密钥并让旧公钥失效。
- 过去曾在聊天中暴露的 GitHub PAT 应立即撤销并重新生成；不要把旧 token 放进脚本或 URL。

## 维护者构建 EXE

在仓库根目录执行：

```powershell
powershell -ExecutionPolicy Bypass -File .\tools\build_windows_app.ps1
```

生成的 `dist\FProCloudStudio.exe` 可以直接交给 Windows 操作者。构建后的 EXE 不包含 Chrome CDP/websocket 自动化代码。

更完整的脚本参数、架构映射和手动接收器说明见 [`README.md`](README.md)。
