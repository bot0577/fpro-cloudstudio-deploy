# FProCloudStudio 一键使用说明

## 日常只需四步

1. 在浏览器中登录 Cloud Studio，打开目标工作区并进入终端；让终端保持可见。
2. 双击同目录下的 `FProCloudStudio.exe`（或双击你收到的独立 EXE）。
3. 在程序中输入一次配置包密码；临时端口保持空白。
4. 点击“开始一键部署”，等待状态变为“完成”。

正常情况下，不需要打开 PowerShell，不需要安装 Python/OpenSSL，不需要手动下载文件，
也不需要复制任何安装命令。程序会自动完成：

- 从 GitHub Raw 下载配置和当前 Windows 架构的加密 `fpro-client`；
- 在本机临时目录解密并读取 mTLS 配置；
- 生成一次性传输 token，建立临时 fpro 通道；
- 通过已打开的 Chrome CDP 自动把安装流程、密码和 token 输入 Cloud Studio 终端；
- 让容器安装 `sshd`、关闭密码登录、生成 root SSH 密钥；
- 只接收加密的 SSH 私钥包，在本机校验、解密并写入密钥文件；
- 完成后自动清理 token、密码临时文件和临时通道。

SSH 私钥默认写入：

```text
%USERPROFILE%\\.ssh\\fpro-cloudstudio
%USERPROFILE%\\.ssh\\fpro-cloudstudio.pub
```

之后使用程序日志中显示的 SSH 主机和映射端口连接。临时“密钥接收”端口不是 SSH
登录端口，不要混用。

## 只有自动输入失败时才需要手动操作

如果程序提示“没有找到带终端的 Cloud Studio 浏览器页面”：

1. 确认 Cloud Studio 终端已经打开并可见，然后重新点击“开始一键部署”；或
2. 等待程序显示“临时通道已建立”后，点击“复制 Cloud Studio 指令”，在终端粘贴
   一次并回车；密码填写刚才输入的同一个密码；出现 token 提示时，点击程序中的
   “复制一次性 token”再粘贴。

这只是 CDP 不可用时的兜底流程，正常路径不需要这些命令。程序会自动尝试常见的
本机 CDP 端口（包括 `9222`、`9333` 等）。

## 常见提示

- `临时远端端口`：留空即可。只有服务端拒绝自动选择的端口时，才在“高级设置”中
  填写一个服务端允许且未占用的端口。
- `目标密钥已存在`：程序默认不覆盖已有密钥。确认后可在高级设置换一个密钥名。
- `临时 fpro 端口未上线`：检查 Cloud Studio 工作区是否已启动、网络是否可访问，
  再换一个临时端口重试。
- `端口已响应`只表示映射端口存活；真正完成还要看到本机“私钥已写入”。

## 安全注意

- 配置密码只在本次运行中使用，不会保存到设置文件。
- Git/GitHub 只放加密载荷和按平台单独加密的二进制；明文证书、客户端私钥、auth
  token、bridge 配置和 SSH 私钥不能提交。
- 不要把不带 `.pub` 后缀的 SSH 私钥上传到仓库、工单或聊天记录；`.pub` 是对应
  公钥，只用于写入目标容器的 `authorized_keys`。
- 之前在聊天中暴露过的 GitHub PAT 必须立即撤销并重新生成；不要继续使用旧值。

## 维护者才需要的内容

日常操作者不需要构建 EXE。维护者在仓库根目录构建：

```powershell
powershell -ExecutionPolicy Bypass -File .\\tools\\build_windows_app.ps1
```

生成的 `dist\\FProCloudStudio.exe` 可直接复制给操作者。更完整的手动/排障说明见
[`README.md`](README.md)。
